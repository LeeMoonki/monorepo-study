{
  "extends": "./tsconfig.build.json",
  "compilerOptions": {
    "jsx": "react-jsx", // 'React' refers to a UMD global, but the current file is a module. Consider adding an import instead.
    "baseUrl": ".",
    "paths": {
      // "@service/*": ["packages/*/src"]
    },
  }
}
